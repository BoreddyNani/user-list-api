name: Deploy to Render

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    # Only deploy if the CI workflow passed (optional but recommended)
    needs: []   # add: [test] if you merge ci and deploy into one workflow

    steps:
      - name: Trigger Render Deploy Hook
        run: |
          curl -X POST "https://api.render.com/deploy/srv-d8stkf4m0tmc73blbmrg?key=0JCMGp9VpEM" \
            --fail \
            --silent \
            --show-error
          echo "Deploy triggered successfully"